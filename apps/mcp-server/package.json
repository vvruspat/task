{
  "name": "@task/mcp-server",
  "version": "0.0.0",
  "private": true,
  "description": "MCP server application for tAsk.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('No tests yet for @task/mcp-server empty scaffold')\""
  }
}
