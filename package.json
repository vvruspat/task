{
  "name": "task",
  "version": "0.0.0",
  "private": true,
  "description": "tAsk monorepo",
  "license": "UNLICENSED",
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "npm run typecheck && turbo run build",
    "lint": "biome check . && npm run typecheck && turbo run lint",
    "lint:fix": "biome check --write . && turbo run lint:fix",
    "format": "biome format --write . && turbo run format",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "generate:openapi": "turbo run generate:openapi",
    "generate:api-client": "npm run generate:openapi && turbo run generate:api-client"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "turbo": "^2.10.3",
    "typescript": "^6.0.3"
  }
}
